Goal: Apply all previous UI/UX improvements + new Upload Documents redesign for visual consistency and brand alignment.
Maintain existing app logic and dummy data; focus purely on layout, colors, spacing, and component consistency.

1. Sidebar Improvements

(same as before)

Remove “Client Portal” text.

Keep only “PlyLegal” logo/title.

Use dark slate gray background (#1F2937) with light gray text.

Active link: green background #285646 and 4px green border-left.

Maintain sticky full-height sidebar.

2. Redundant Headline Fix

(same as before)

Only one main header per page.

Remove duplicated “Visa Applications” title.

3. Back Navigation

(same as before)

Add “Back to Visa Applications” link on all intake and review pages.

4. Review & PDF Page Redesign

(as shared earlier)

Remove right sidebar.

Improve search visibility.

Show all questions and responses (including blanks).

Align Logout and overall spacing consistently.

Add proper section styling and better hierarchy.

5. Upload Documents Page — Full Redesign
🔧 Requirements

Improve layout consistency and match the app’s green-gray palette.

Replace the flat table with a modern bordered container and soft shadows.

Ensure clear spacing (py-3 px-4 on all cells).

Align Upload button right with consistent style.

🎨 Status Badge System
Status	Color	Badge Style
Pending	Gray	bg-gray-100 text-gray-700
Under Review	Yellow	bg-yellow-100 text-yellow-700 border border-yellow-300
Approved	Green	bg-green-100 text-green-700 border border-green-300
Rejected	Red	bg-red-100 text-red-700 border border-red-300

Example:

<span class="px-2 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700 border border-yellow-300">
  Under Review
</span>

📁 Upload Button
<button class="px-3 py-1.5 border border-[#285646] text-[#285646] rounded-md text-xs font-medium hover:bg-[#285646] hover:text-white transition">
  <i class="ri-upload-cloud-line mr-1"></i> Upload
</button>

🧩 Container

Add light gray background for the whole section: bg-gray-50.

Table inside a rounded white card with soft border and shadow.

Keep upload instruction text below page title.

6. Global Consistency Across All Pages

✅ Color Palette

Primary: #285646 (PlyLegal green)

Secondary: #00A67E (accent green)

Neutrals: #F3F4F6 (background), #E5E7EB (borders), #1F2937 (dark text)

✅ Typography

Headings: font-semibold text-gray-900

Labels / Status: text-sm font-medium

Content: text-sm text-gray-700

✅ Spacing

Use consistent margins and paddings (px-6 py-8 main content).

✅ Buttons

Uniform border-radius (rounded-md), same hover transitions.

✅ Layout

Logout stays top-right on every page.

Sidebar is sticky.

No right sidebar in any page.

Acceptance Criteria
Area	Change
Sidebar	Clean, professional, brand-colored
Headings	No duplicates
Navigation	Back links present
Review & PDF	Consistent layout, visible questions
Upload Documents	New badge system + improved table
Status Colors	Consistent and brand-aligned
Buttons	Uniform across app
Layout	Logout and spacing consistent everywhere